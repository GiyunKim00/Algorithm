#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--){
        int n, m;
        cin >> n >> m;
        cout << n - 1 << "\n";
        while (m--){
            int a, b;
            cin >> a >> b;
        }
    }

    return 0;
}